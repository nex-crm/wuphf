# File-size allowlist — files currently over 1500 LOC that are tracked
# for decomposition. See docs/CODE-QUALITY.md for the patterns and
# CONTRIBUTING.md for the rule.
#
# This file is FORWARD-ONLY. Entries can shrink or be removed; new entries
# are a violation. Every refactor PR must include the allowlist diff in
# its description.
#
# Format: one path (relative to repo root) per line. Comments start with #.
# Blank lines and trailing whitespace ignored.

# Tier 1 — refactor targets in the active migration (see plan)
cmd/wuphf/channel.go                          # Phase 2: split into channel_<concern>.go siblings
internal/team/broker.go                       # Phase 3: extract sse/auth/rate_limit/wiki/telemetry/web_proxy/publish
internal/team/broker_office_channels.go       # Phase 4: split by operation type (crud/members/kinds)

# Other current offenders — to be addressed in follow-up PRs after Phase 1-9
# baseline is in place. Each line documents the intended approach.
internal/teammcp/server.go                    # vertical slice along tool families
internal/team/broker_tasks.go                 # vertical slice (already split once; further breakdown along action vs query)
internal/team/operation_bootstrap.go          # split per bootstrap concern (db/manifest/seed)
web/src/components/apps/SkillsApp.tsx         # decompose alongside SettingsApp/Wizard pattern (per-section panels in skills/)
