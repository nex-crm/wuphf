# Screenshots for PR #1158

Captured by `web/e2e/screenshots/publish.sh agent-routines 1158` from
`agent-routines.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
