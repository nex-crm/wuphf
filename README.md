# Screenshots for PR #892

Captured by `web/e2e/screenshots/publish.sh onboarding-phase2 892` from
`onboarding-phase2.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
