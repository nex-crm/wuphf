# Screenshots for PR #873

Captured by `web/e2e/screenshots/publish.sh rich-artifact-chat 873` from
`rich-artifact-chat.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
