# Screenshots for PR #1063

Captured by `web/e2e/screenshots/publish.sh agent-instruction-files 1063` from
`agent-instruction-files.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
