# Screenshots for PR #1001

Captured by `web/e2e/screenshots/publish.sh agent-providers-integrations 1001` from
`agent-providers-integrations.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
