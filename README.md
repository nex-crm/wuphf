# Screenshots for PR #1087

Captured by `web/e2e/screenshots/publish.sh consolidate-inbox-tasks 1087` from
`consolidate-inbox-tasks.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
