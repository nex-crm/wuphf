# Screenshots for PR #993

Captured by `web/e2e/screenshots/publish.sh reviews-dm-scroll-fixes 993` from
`reviews-dm-scroll-fixes.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
