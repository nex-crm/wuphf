# Screenshots for PR #970

Captured by `web/e2e/screenshots/publish.sh scan-chip-desync-fix 970` from
`scan-chip-desync-fix.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
