# Screenshots for PR #911

Captured by driving a real `wuphf` binary on `--web-port 7920 --broker-port 7921`
through the full `greet → blueprint` onboarding flow with a fresh
`WUPHF_RUNTIME_HOME`, using Playwright against the live server (not the
mocked vite harness under `web/e2e/screenshots/`).

Safe to delete after the PR merges.
