# Screenshots for PR #1075

Captured by `web/e2e/screenshots/publish.sh agent-subspace 1075` from
`agent-subspace.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
