# Screenshots for PR #1016

Captured by `web/e2e/screenshots/routines-filter.mjs` against vite dev with
mocked broker responses. Before = classifier reverted to origin/main; after =
the fix. Safe to delete after the PR merges.
