# Screenshots for PR #1085

Captured by `web/e2e/screenshots/publish.sh prepick-gif-layout 1085` from
`prepick-gif-layout.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
