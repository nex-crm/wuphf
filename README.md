# Screenshots for PR #1106

Captured by `web/e2e/screenshots/publish.sh apps-build-gate 1106` from
`apps-build-gate.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
