# Screenshots for PR #1067

Captured by `web/e2e/screenshots/publish.sh provider-selection 1067` from
`provider-selection.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
