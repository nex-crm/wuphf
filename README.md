# Screenshots for PR #1012

Captured by `web/e2e/screenshots/publish.sh inbox-needs-action 1012` from
`inbox-needs-action.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
