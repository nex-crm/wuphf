# Screenshots for PR #1054

Captured by `web/e2e/screenshots/publish.sh pam-avatar-consistency 1054` from
`pam-avatar-consistency.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
