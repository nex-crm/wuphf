# Screenshots for PR #1077

Captured by `web/e2e/screenshots/publish.sh analytics-consent 1077` from
`analytics-consent.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
