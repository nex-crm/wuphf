# Screenshots for PR #1055

Captured by `web/e2e/screenshots/publish.sh remove-receipts-console 1055` from
`remove-receipts-console.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
