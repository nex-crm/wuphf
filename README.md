# Screenshots for PR #890

Captured by `web/e2e/screenshots/publish.sh harness-badges 890` from
`harness-badges.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
