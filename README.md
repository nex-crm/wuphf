# Screenshots for PR #1008

Captured by `web/e2e/screenshots/publish.sh theme-contrast 1008` from
`theme-contrast.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
