{
  "name": "wuphf-dev-tooling",
  "private": true,
  "description": "Root-level dev tooling (secretlint, commitlint). The Go v0 app lives at the repo root; web UI lives in ./web with its own package.json; v1 protocol package lives in ./packages/protocol; installer-stub + release pipeline live in ./apps/installer-stub.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "installer:build:dry-run": "bun run --cwd apps/installer-stub build:dry-run",
    "installer:build:local": "bun run --cwd apps/installer-stub build:current",
    "installer:check:secrets": "bun run --cwd apps/installer-stub check:secrets",
    "installer:lint": "bun run --cwd apps/installer-stub lint"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.0",
    "@commitlint/config-conventional": "^21.0.0",
    "@secretlint/secretlint-rule-pattern": "^13.0.0",
    "@secretlint/secretlint-rule-preset-recommend": "^13.0.0",
    "secretlint": "^13.0.0"
  },
  "version": "0.0.0"
}
