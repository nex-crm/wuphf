{
  "name": "wuphf-dev-tooling",
  "private": true,
  "description": "Root-level dev tooling (secretlint, commitlint). The Go v0 app lives at the repo root; web UI lives in ./web with its own package.json; v1 protocol package lives in ./packages/protocol.",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@secretlint/secretlint-rule-pattern": "^12.2.0",
    "@secretlint/secretlint-rule-preset-recommend": "^12.2.0",
    "secretlint": "^12.2.0"
  },
  "version": "0.0.0"
}
