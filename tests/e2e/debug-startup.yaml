session:
  command: ./wuphf
  cols: 120
  rows: 40
artifacts:
  dir: ./termwright-artifacts

steps:
  - waitForText:
      text: "Loading"
      timeoutMs: 5000

  - screenshot:
      name: "debug-loading"
