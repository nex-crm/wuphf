session:
  command: ./wuphf
  cols: 120
  rows: 40
artifacts:
  dir: ./termwright-artifacts

steps:
  # AC: /quit exits cleanly
  - waitForText:
      text: "Type a message"
      timeoutMs: 5000

  - type:
      text: "/quit"

  - press:
      key: Enter

  - sleep:
      ms: 1000

  - screenshot:
      name: "ac07-quit"
